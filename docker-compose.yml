services:
  wifi-support:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=cle_secrete_production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
